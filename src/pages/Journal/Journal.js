import React, { useEffect, useState } from "react";
import { Link, useLocation } from "react-router-dom";
import Breadcrumbs from "../../components/pageProps/Breadcrumbs";

const Journal = () => {
  const location = useLocation();
  const [prevLocation, setPrevLocation] = useState("");
  useEffect(() => {
    setPrevLocation(location.state.data);
  }, [location]);
  return (
    <div className="max-w-container mx-auto px-4">
      <Breadcrumbs title="Journals" prevLocation={prevLocation} />
      <div className="pb-10">
        <h1 className="max-w-[600px] text-base text-lightText mb-2">
          <span className="text-primeColor font-semibold text-lg">Unique</span>{" "}
          is your gateway to a world of distinctive shopping experiences, all from the palm of your hand. 
          Our app offers a sleek, user-friendly interface that curates exclusive products and personalized recommendations just for you. 
          Explore innovative finds and rare treasures, from global artisans and cutting-edge designers, all in one place. 
          With UNIQUE discover, shop, and indulge in a shopping adventure that’s truly one-of-a-kind.
        </h1>
        <Link to="/shop">
          <button className="w-52 h-10 bg-primeColor text-white hover:bg-black duration-300">
            Continue Shopping
          </button>
        </Link>
      </div>
    </div>
  );
};

export default Journal;
